"""Application-wide constants and default values."""

# F1TENTH F110 RC car dimensions (meters)
CAR_LENGTH_M = 0.434  # nose-to-tail length
CAR_WIDTH_M = 0.1738  # side-to-side width
SPAWN_HEADING_LENGTH_M = 0.5  # visual heading indicator length

# Default window dimensions (pixels)
DEFAULT_WINDOW_WIDTH = 1280
DEFAULT_WINDOW_HEIGHT = 720

# Track analysis defaults
DEFAULT_CENTERLINE_SPACING = 0.2  # meters between centerline samples
DEFAULT_TRACK_WIDTH_TARGET = 2.2  # meters for auto-scale calculation
